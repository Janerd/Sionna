# Sionna RT 移动性仿真项目依赖
# 安装方式：pip install -r requirements.txt
#
# 注意：PyTorch 需要单独安装（见 setup.md 第四步），
# 因为需要指定 CUDA 版本的安装源。
# 本文件只包含 PyTorch 之外的依赖。


# ===== 数值计算 =====
numpy>=1.24.0          # 数值计算基础库
scipy>=1.10.0          # 科学计算（插值、信号处理）

# ===== 数据处理 =====
pandas>=2.0.0          # 数据分析（结果汇总）
h5py>=3.8.0            # HDF5 文件读写（兼容 MATLAB .mat v7.3）

# ===== 可视化 =====
matplotlib>=3.7.0      # 绘图（轨迹、RSRP 时序、信道特征）

# ===== 进度显示 =====
tqdm>=4.65.0           # 进度条

# ===== 机器学习（GRU 训练，与 MATLAB 项目共用）=====
# torch 已单独安装，此处不重复
scikit-learn>=1.3.0    # 数据预处理、评估指标